expname: column20260525_twostage_lowrecon_lazyregSRskip_drift_interp_feature_changegru_gloss
data:
  datadir: [data/RS307_n, data/RS330_n, data/RS615_n]
  extractor_path: /Data/home/vicky/graf260518_im64/HystereticGRU/2026-05-24_16-19-37/Opensees Based damage Patterns Generation/s3mcwmow/checkpoints/best.ckpt
  far: 4.5
  fov: 20
  imsize: 256  
  near: 1.5
  orthographic: False
  use_default_rays: False
  radius: 2.5
  type: RS307_0_i2
  umax: 1.
  umin: 0.
  vmax: 0.5
  vmin: 0.
  v: 0.5, 0.4, 0.3, 0.2 
discriminator:
  hflip: false
  ndf: 64
  hidden_dim: 1024
  cond_dim: 256
nerf:
  N_samples: 64
  decrease_noise: true
  i_embed: 0
  multires: 10
  multires_views: 4
  netdepth: 8
  netwidth: 256
  perturb: 1.0
  raw_noise_std: 1.0
  use_viewdirs: true
  N_importance: 0
  netdepth_fine: 8
  netwidth_fine: 256
  num_classes: 7
ray_sampler:
  N_samples: 4096          # 64×64 = 4096
  max_scale: 1.0
  min_scale: 0.25
  scale_anneal: 0.01
training:
  backup_every: 50000
  batch_size: 8
  chunk: 32768
  equalize_lr: false
  fid_every: 10000
  gan_type: standard
  # ========================================================
  # [修正] 反向 TTUR：G 學快、D 學慢，解決 D 太強問題
  # ========================================================
  lr_g: 0.0003
  lr_d: 0.0001
  lr_anneal: 0.5
  lr_anneal_every: 150000,220000,2800000
  model_average_beta: 0.999
  model_average_reinit: false
  model_file: model.pt
  netchunk: 65536
  nworkers: 0
  optimizer: rmsprop
  outdir: ./results
  print_every: 10
  reg_param: 10.0
  # ========================================================
  # [修正] aux/mat loss 權重分開控制
  # ========================================================
  label_param: 0.5         # aux_loss (hidden_state 預測) 權重
  mat_param: 0.1           # [新增] mat_loss (材質 label 預測) 權重
  restart_every: -1
  sample_every: 5000
  save_best: fid
  save_every: 1000
  take_model_average: true
  use_amp: true
  amp_dtype: bfloat16
  # ========================================================
  # [兩階段設定]
  # ========================================================
  phase2_start_iter: 100000
  lambda_d_high: 1.0
  lambda_recon_sr: 1.0
z_dist:
  dim: 256
  type: gauss
ccsr: 
  enabled: false
  num_views: 8
  scale_factor: 1
# ========================================================
# [修正] recon loss 大幅降低，避免壓過對抗信號
# ========================================================
reconstruction:
  enabled: true
  lambda_recon: 0.15       # 從 0.5 降到 0.05
wandb:
  project: graf260108
  entity: vicky20020808
  name: column20260525_twostage_lowrecon_lazyregSRskip_drift_interp_feature_changegru_gloss
interpolation:
  enabled: true
  n_samples: 2
  lambda: 0.5
  start_iter: 5000